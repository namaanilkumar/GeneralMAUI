<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="GeneralMAUI.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:GeneralMAUI"
    xmlns:custom="clr-namespace:GeneralMAUI.Views.Authentication"
    xmlns:cust="clr-namespace:GeneralMAUI.Views.Grid"
     xmlns:em="clr-namespace:GeneralMAUI.Views.Employee"
    Shell.FlyoutBehavior="Flyout"
    Title="GeneralMAUI">

    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />-->


    <ShellContent
  Title="Employee"
  ContentTemplate="{DataTemplate em:EmployeeDetailPage}"
  Route="EmployeeDetailPage" />

    <ShellContent
Title="Login"
ContentTemplate="{DataTemplate custom:LoginTwo}"
Route="LoginTwo" />


    <ShellContent
 Title="Grid"
 ContentTemplate="{DataTemplate cust:GridOne}"
 Route="GridOne" />

    <ShellContent
 Title="Main"
 ContentTemplate="{DataTemplate local:MainPage}"
 Route="MainPage" />

    <ShellContent
     Title="LoginMain"
     ContentTemplate="{DataTemplate custom:LoginPage}"
     Route="Login" />





</Shell>
